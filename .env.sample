# RealtimeVoiceClient Multi-Agent Orchestrator
# Copy this file to .env and fill in your actual values

# ============================================================
# OpenAI Realtime API (Required)
# ============================================================
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# ============================================================
# Backend Server Configuration (Required)
# ============================================================
# The WebSocket URL for the OpenAI Realtime API compatible backend
# Default: ws://192.168.50.40:8000/v1/realtime?model=gpt-realtime
BACKEND_URL=ws://192.168.50.40:8000/v1/realtime?model=gpt-realtime
API_KEY=test-key

# ============================================================
# Claude Code Agent (Optional - uses CLI, not API)
# ============================================================
# Claude Code uses your Claude Code subscription ($20/month)
# No API key needed - just install the CLI:
#   npm install -g @anthropic-ai/claude-code
#   claude --dangerously-skip-permissions
#
# Optional: Specify custom path to claude CLI
# CLAUDE_CLI_PATH=/path/to/claude

# ============================================================
# Gemini Browser Agent (Optional)
# ============================================================
# Get your API key from: https://aistudio.google.com/app/apikey
# GEMINI_API_KEY=...

# Required Python packages for Gemini:
#   pip install google-generativeai playwright
#   playwright install

# ============================================================
# Agent Zero (Optional)
# ============================================================
# Agent Zero API endpoint and credentials
# AGENT_ZERO_API_URL=http://localhost:5000
# AGENT_ZERO_API_KEY=...

# Required Python packages for Agent Zero:
#   pip install requests

# ============================================================
# Agent Configuration
# ============================================================
# Workspace directory for all agents (default: ./workspace)
# AGENT_WORKING_DIRECTORY=/path/to/workspace

# ============================================================
# Notes
# ============================================================
# Required for basic functionality:
#   - OPENAI_API_KEY (for voice orchestration)
#   - BACKEND_URL (for Realtime API connection)
#
# Optional for specific agent types:
#   - GEMINI_API_KEY (for browser automation)
#   - AGENT_ZERO_API_URL (for general AI tasks)
#   - Claude CLI (installed separately, no env var needed)
